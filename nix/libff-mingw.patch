--- a/libff/common/profiling.cpp
+++ b/libff/common/profiling.cpp
@@ -42,7 +42,11 @@ long long get_nsec_cpu_time()
     struct timespec ts;
 #endif
 
+#ifdef _WIN32
+    return 0; // Profiling not supported on Windows
+#else
     if ( ::clock_gettime(CLOCK_PROCESS_CPUTIME_ID, &ts) )
+#endif
     {
         return -1;
     }